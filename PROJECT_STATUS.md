# PROJECT STATUS

## 现在在干什么

正在重构画布渲染底层，目标是一次性把主地图从“复杂 SVG 长期承担显示和缩放”改成更适合大线网的渲染架构。

当前工作继续放在 `refactor/render-scene-foundation` / PR #42 上，不把中间半成品提前合进 `main`。这一轮会把共享绘制数据、Canvas 主画面、编辑交互层和现有 SVG 导出链路一起接通后再验收。

## 已经完成

- PR #40 的临时 PNG 栅格快照缩放实验已经完整回滚，`main` 已恢复到实验前的 SVG 即时缩放方案（PR #41）。
- 已开始把“地图最终应该画成什么”从 React/SVG 组件中拆出来，形成后续不同渲染器可以共用的绘制数据。
- 线路的时间状态、线路样式和路径计算已经先从 SVG 组件中抽离；现有 SVG 仍按原效果显示。
- 站点的可见范围、筛选和选中状态也已经开始移出 JSX。
- 已为这部分底层拆分补充回归测试（PR #42）。

## 做过但还需要修

- 现有连续缩放仍然使用整棵复杂 SVG 做即时变换；在大工程里仍有明显卡顿。
- PR #40 证明“临时生成 PNG、滚轮时在 PNG 和 SVG 之间切换”的实现方式不合适，已经回滚，不再沿这条实现继续修。
- 当前的共享绘制层还只覆盖了重构的第一部分，尚未形成完整的 Canvas 主渲染链路。
- 停止缩放后的最终视图提交仍可能触发较重的整图计算，这部分要随新渲染底层一起处理，而不是继续单独调滚轮参数。

## 还要做

- 把线路、站点、换乘站、站名、AARC textTags、地形/道路、地图元素等正式视觉内容统一接入共享绘制层。
- 建立 Canvas 主渲染器，让大地图视觉不再长期依赖大量 SVG DOM。
- 保留轻量 SVG/DOM 编辑交互层，用于点击、选中、控制点、拖动手柄等编辑行为。
- 参考 AARC 的实际做法完成平移/缩放链路：交互过程中复用已有 Canvas，停止后按当前视口重新精确绘制。
- 加入视口附近绘制和必要缓冲，避免为了屏幕外内容持续付出整图绘制成本。
- 拖动时继续采用稳定主画面 + 小范围活动层，避免一次编辑导致整张地图重画。
- 保住现有 SVG 矢量导出；Canvas 只负责编辑器主画面，不能把正式导出降成位图。
- 验证 Presentation、Video、AARC 导入语义、支线、复合站和历史时间语义没有被底层重构破坏。
- 用常陵等大工程实际验收缩放、平移、拖动和最终提交性能。

## 不要做

- 不再继续 PR #40 那种“空闲时生成 PNG，滚轮时 PNG ↔ SVG 来回切换”的方案。
- 不再把主要精力放在继续微调滚轮灵敏度、缓动等表面参数上来掩盖底层性能问题。
- 不把整个项目推倒重写；数据模型、AARC 导入语义和已有编辑能力尽量保持不变。
- 不为了 Canvas 主画面牺牲 SVG 矢量导出。
- 不把底层重构拆成多个半成品依次合入 `main`；本轮先在 PR #42 分支完整做完再统一验收。

## 接下来怎么做

1. 继续扩完整共享绘制层，让正式地图视觉不再直接依赖 SVG JSX 才能计算出来。
2. 在同一分支接入 Canvas 主渲染，并保留现有 SVG 作为对照和正式导出基础。
3. 把编辑命中、选中和控制点拆成轻量交互层，避免 Canvas 改造破坏编辑能力。
4. 接入 AARC 式视口绘制、交互复用和停止后重绘。
5. 完整跑现有回归测试，并补 Canvas/共享绘制层测试。
6. 用大工程实际对比缩放、平移和拖动表现；确认视觉与编辑没有明显回退后，再处理 PR #42 的最终合并。
