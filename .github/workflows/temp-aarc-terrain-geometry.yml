name: Temp AARC terrain geometry fidelity

on:
  push:
    branches:
      - fix/aarc-terrain-geometry-fidelity

permissions:
  contents: write

jobs:
  patch-test-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.19.0
          cache: npm
      - name: Apply focused AARC terrain compatibility patch
        shell: bash
        run: |
          python <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{path}: expected one match, found {count}')
              p.write_text(text.replace(old, new), encoding='utf-8')

          model_old = "export interface BasemapPathPoint { id: string; x: number; y: number }\nexport type BasemapPathCategory = 'water' | 'terrain' | 'other'\nexport interface BasemapPath { id: string; name?: string; category: BasemapPathCategory; points: BasemapPathPoint[]; color: string; width: number; opacity: number; closed: boolean; isFilled: boolean; zIndex: number; visible: boolean; locked: boolean; source?: SourceMetadata }"
          model_new = """export interface BasemapPathPoint {
  id: string
  x: number
  y: number
  /** Original AARC point semantics, retained so imported terrain can be re-formalized exactly. */
  aarcPointId?: number
  aarcDir?: 0 | 1
  aarcFree?: boolean
}
export type BasemapPathCategory = 'water' | 'terrain' | 'other'
export type BasemapPathLineCap = 'butt' | 'round' | 'square'
export interface AarcBasemapGeometry {
  kind: 'aarc'
  lineTurnAreaRadius: number
  lineWidthBase: number
  lineCarpetWiden: number
  backgroundColor: string
  removeCarpet?: boolean
}
export type BasemapPathGeometry = AarcBasemapGeometry
export interface BasemapPath { id: string; name?: string; category: BasemapPathCategory; points: BasemapPathPoint[]; color: string; width: number; opacity: number; closed: boolean; isFilled: boolean; zIndex: number; visible: boolean; locked: boolean; lineCap?: BasemapPathLineCap; geometry?: BasemapPathGeometry; source?: SourceMetadata }"""
          replace_once('src/data/model.ts', model_old, model_new)

          Path('src/data/aarcBasemapGeometry.ts').write_text(r'''import type { BasemapPath, BasemapPathPoint } from './model'

const EPS = 1e-4
const TURN_45_RATIO = 2.4142135 * .618

type Dir = 0 | 1
type PosRel = 's' | 'l' | 'llu' | 'lu' | 'luu' | 'u' | 'uur' | 'ur' | 'urr'
type WayRel = 'parallel' | '90' | '45' | '135'
type Coord = { x: number; y: number }

interface ControlPoint extends Coord {
  key: string | number
  dir: Dir
  free: boolean
}

interface FormalSeg {
  a: Coord
  itp: Coord[]
  b: Coord
  ill: number
  direct?: boolean
  aFree?: boolean
  bFree?: boolean
}

export interface AarcBasemapFormalPoint extends Coord {
  free?: boolean
}

/** Port of AARC formalize.ts for imported terrain control points. */
export function formalizeAarcBasemapPoints(points: BasemapPathPoint[]): AarcBasemapFormalPoint[] {
  const controls: ControlPoint[] = points.map(point => ({
    key: Number.isFinite(point.aarcPointId) ? point.aarcPointId! : point.id,
    x: point.x,
    y: point.y,
    dir: point.aarcDir === 1 ? 1 : 0,
    free: point.aarcFree === true,
  }))
  if (controls.length < 2) return controls.map(point => ({ x: point.x, y: point.y, ...(point.free ? { free: true } : {}) }))

  const ring = controls.length > 2 && controls[0].key === controls.at(-1)!.key
  const segs: FormalSeg[] = []
  if (!ring) {
    for (let i = 0; i < controls.length - 1; i += 1) segs.push(formalizeSeg(controls[i], controls[i + 1]))
  } else {
    segs.push(formalizeSeg(controls[controls.length - 2], controls[0]))
    for (let i = 0; i < controls.length - 1; i += 1) segs.push(formalizeSeg(controls[i], controls[i + 1]))
    segs.push(formalizeSeg(controls.at(-1)!, controls[1]))
  }
  justifyIllPosedSegments(segs)
  if (!segs.length) return []
  if (ring) { segs.shift(); segs.pop() }

  const result: AarcBasemapFormalPoint[] = [{ x: segs[0].a.x, y: segs[0].a.y, ...(segs[0].aFree ? { free: true } : {}) }]
  for (const seg of segs) {
    for (const point of seg.itp) result.push({ x: point.x, y: point.y })
    result.push({ x: seg.b.x, y: seg.b.y, ...(seg.bFree ? { free: true } : {}) })
  }
  return result
}

/** Build the same formalized, rounded path that AARC uses for type=1 terrain. */
export function buildAarcBasemapPathD(path: BasemapPath): string {
  const points = formalizeAarcBasemapPoints(path.points)
  if (!points.length) return ''
  if (points.length === 1) return `M ${fmt(points[0].x)} ${fmt(points[0].y)}`
  const ring = points.length > 2 && sameCoord(points[0], points.at(-1)!)
  const commands: string[] = []

  if (!ring) {
    commands.push(`M ${fmt(points[0].x)} ${fmt(points[0].y)}`)
    for (let i = 1; i < points.length - 1; i += 1) appendCorner(commands, path, points[i - 1], points[i], points[i + 1])
    commands.push(`L ${fmt(points.at(-1)!.x)} ${fmt(points.at(-1)!.y)}`)
    if (path.closed) commands.push('Z')
    return commands.join(' ')
  }

  const unique = points.slice(0, -1)
  if (unique.length < 2) return `M ${fmt(unique[0]?.x ?? points[0].x)} ${fmt(unique[0]?.y ?? points[0].y)}${path.closed ? ' Z' : ''}`
  const firstCorner = cornerGeometry(path, unique.at(-1)!, unique[0], unique[1 % unique.length])
  commands.push(`M ${fmt(firstCorner.before.x)} ${fmt(firstCorner.before.y)}`)
  appendArc(commands, firstCorner.before, unique[0], firstCorner.after, firstCorner.free)
  for (let i = 1; i < unique.length; i += 1) {
    const corner = cornerGeometry(path, unique[(i - 1 + unique.length) % unique.length], unique[i], unique[(i + 1) % unique.length])
    commands.push(`L ${fmt(corner.before.x)} ${fmt(corner.before.y)}`)
    appendArc(commands, corner.before, unique[i], corner.after, corner.free)
  }
  commands.push(`L ${fmt(firstCorner.before.x)} ${fmt(firstCorner.before.y)}`)
  if (path.closed) commands.push('Z')
  return commands.join(' ')
}

function appendCorner(commands: string[], path: BasemapPath, previous: AarcBasemapFormalPoint, current: AarcBasemapFormalPoint, next: AarcBasemapFormalPoint) {
  const corner = cornerGeometry(path, previous, current, next)
  commands.push(`L ${fmt(corner.before.x)} ${fmt(corner.before.y)}`)
  appendArc(commands, corner.before, current, corner.after, corner.free)
}

function cornerGeometry(path: BasemapPath, previous: AarcBasemapFormalPoint, current: AarcBasemapFormalPoint, next: AarcBasemapFormalPoint) {
  const free = previous.free === true || current.free === true || next.free === true
  const incoming = { x: current.x - previous.x, y: current.y - previous.y }
  const outgoing = { x: next.x - current.x, y: next.y - current.y }
  const incomingLength = magnitude(incoming), outgoingLength = magnitude(outgoing)
  if (incomingLength < EPS || outgoingLength < EPS) return { before: current, after: current, free }

  if (free) {
    const u1 = unit(incoming), u2 = unit(outgoing)
    const dot = u1.x * u2.x + u1.y * u2.y
    const cross = u1.x * u2.y - u1.y * u2.x
    if (Math.abs(cross) < EPS) return { before: current, after: current, free }
    const theta = Math.atan2(Math.abs(cross), -dot)
    const radius = getTurnRadius(path, theta)
    const tanHalf = Math.tan(theta / 2)
    const d = Math.min(tanHalf > EPS ? radius / tanHalf : 0, incomingLength / 2, outgoingLength / 2)
    return {
      before: { x: current.x - u1.x * d, y: current.y - u1.y * d },
      after: { x: current.x + u2.x * d, y: current.y + u2.y * d },
      free,
    }
  }

  const rel = wayRel(incoming, outgoing)
  const distance = Math.min(getTurnRadius(path, rel), incomingLength / 2, outgoingLength / 2)
  const beforeWay = unit8({ x: previous.x - current.x, y: previous.y - current.y })
  const afterWay = unit8({ x: next.x - current.x, y: next.y - current.y })
  return {
    before: { x: current.x + beforeWay.x * distance, y: current.y + beforeWay.y * distance },
    after: { x: current.x + afterWay.x * distance, y: current.y + afterWay.y * distance },
    free,
  }
}

function appendArc(commands: string[], before: Coord, via: Coord, after: Coord, free: boolean) {
  const a = { x: via.x - before.x, y: via.y - before.y }
  const b = { x: after.x - via.x, y: after.y - via.y }
  const ua = free ? unit(a) : unit8(a)
  const ub = free ? unit(b) : unit8(b)
  const cross = ua.x * ub.y - ua.y * ub.x
  if (Math.abs(cross) < EPS) {
    commands.push(`L ${fmt(after.x)} ${fmt(after.y)}`)
    return
  }
  const na = { x: -ua.y, y: ua.x }, nb = { x: -ub.y, y: ub.x }
  const denominator = cross2(na, nb)
  if (Math.abs(denominator) < EPS) {
    commands.push(`L ${fmt(after.x)} ${fmt(after.y)}`)
    return
  }
  const delta = { x: after.x - before.x, y: after.y - before.y }
  const t = cross2(delta, nb) / denominator
  const center = { x: before.x + na.x * t, y: before.y + na.y * t }
  const radius = Math.hypot(before.x - center.x, before.y - center.y)
  if (!Number.isFinite(radius) || radius < EPS) {
    commands.push(`L ${fmt(after.x)} ${fmt(after.y)}`)
    return
  }
  const sweep = cross < 0 ? 0 : 1
  commands.push(`A ${fmt(radius)} ${fmt(radius)} 0 0 ${sweep} ${fmt(after.x)} ${fmt(after.y)}`)
}

function getTurnRadius(path: BasemapPath, relation: WayRel | number) {
  const geometry = path.geometry?.kind === 'aarc' ? path.geometry : undefined
  const base = geometry?.lineTurnAreaRadius ?? 30
  let radius = Math.max(0, base + path.width / 2)
  if (typeof relation === 'number') {
    if (isZero(relation - Math.PI / 4)) radius /= TURN_45_RATIO
    else if (isZero(relation - 3 * Math.PI / 4)) radius *= TURN_45_RATIO
  } else if (relation === '45') radius /= TURN_45_RATIO
  else if (relation === '135') radius *= TURN_45_RATIO
  return radius
}

function wayRel(a: Coord, b: Coord): WayRel {
  const aw = signWay(a), bw = signWay(b)
  const cross = cross2(aw, bw)
  if (isZero(cross)) return 'parallel'
  const dot = aw.x * bw.x + aw.y * bw.y
  if (isZero(dot)) return '90'
  return dot > 0 ? '45' : '135'
}

function formalizeSeg(originalA: ControlPoint, originalB: ControlPoint): FormalSeg {
  if (originalA.free || originalB.free) return { a: coord(originalA), itp: [], b: coord(originalB), ill: 0, direct: true, aFree: originalA.free, bFree: originalB.free }
  let a = originalA, b = originalB
  let xDiff = a.x - b.x, yDiff = a.y - b.y
  const rel = coordRelDiff(xDiff, yDiff), pr = rel.posRel, reversed = rel.rev
  if (pr === 's') return { a: coord(originalA), itp: [], b: coord(originalB), ill: 0, aFree: originalA.free, bFree: originalB.free }
  if (reversed) {
    ;[a, b] = [b, a]
    xDiff = -xDiff
    yDiff = -yDiff
  }
  let itp: Coord[] = []
  let ill = 0
  if (a.dir === b.dir) {
    itp = coordFill(a, b, xDiff, yDiff, pr, reversed, a.dir === 1 ? 'midVert' : 'midInc')
    if (!itp.length) {
      if ((a.dir === 0 && (pr === 'lu' || pr === 'ur')) || (a.dir === 1 && (pr === 'l' || pr === 'u'))) ill = 2
    } else ill = 1
  } else if (a.dir === 1) {
    itp = coordFill(a, b, xDiff, yDiff, pr, reversed, pr === 'luu' || pr === 'uur' ? 'top' : 'bottom')
  } else {
    itp = coordFill(a, b, xDiff, yDiff, pr, reversed, pr === 'luu' || pr === 'uur' ? 'bottom' : 'top')
  }
  return { a: coord(originalA), itp, b: coord(originalB), ill, aFree: originalA.free, bFree: originalB.free }
}

function coordRelDiff(xDiff: number, yDiff: number): { posRel: PosRel; rev: boolean } {
  if (isZero(xDiff)) {
    if (isZero(yDiff)) return { posRel: 's', rev: false }
    return { posRel: 'u', rev: yDiff > 0 }
  }
  if (isZero(yDiff)) return { posRel: 'l', rev: xDiff > 0 }
  if (isZero(xDiff - yDiff)) return { posRel: 'lu', rev: xDiff > 0 }
  if (isZero(xDiff + yDiff)) return { posRel: 'ur', rev: yDiff > 0 }
  if ((yDiff > 0 && xDiff > yDiff) || (yDiff < 0 && xDiff < yDiff)) return { posRel: 'llu', rev: yDiff > 0 }
  if ((xDiff > 0 && yDiff > xDiff) || (xDiff < 0 && yDiff < xDiff)) return { posRel: 'luu', rev: xDiff > 0 }
  if ((yDiff > 0 && -xDiff < yDiff) || (yDiff < 0 && xDiff < -yDiff)) return { posRel: 'uur', rev: yDiff > 0 }
  return { posRel: 'urr', rev: xDiff < 0 }
}

function coordFill(a: Coord, b: Coord, xDiff: number, yDiff: number, posRel: PosRel, reversed: boolean, type: 'top' | 'bottom' | 'midVert' | 'midInc'): Coord[] {
  const result: Coord[] = []
  if (posRel === 'l' || posRel === 'u' || posRel === 'lu' || posRel === 'ur') return result
  if (posRel === 'llu') {
    if (type === 'top') { const bias = -xDiff + yDiff; result.push({ x: a.x + bias, y: a.y }) }
    else if (type === 'bottom') { const bias = -xDiff + yDiff; result.push({ x: b.x - bias, y: b.y }) }
    else if (type === 'midInc') { const bias = (-xDiff + yDiff) / 2; result.push({ x: a.x + bias, y: a.y }, { x: b.x - bias, y: b.y }) }
    else { const bias = -yDiff / 2; result.push({ x: a.x + bias, y: a.y + bias }, { x: b.x - bias, y: b.y - bias }) }
  } else if (posRel === 'luu') {
    if (type === 'top') { const bias = xDiff - yDiff; result.push({ x: b.x, y: b.y - bias }) }
    else if (type === 'bottom') { const bias = xDiff - yDiff; result.push({ x: a.x, y: a.y + bias }) }
    else if (type === 'midInc') { const bias = (xDiff - yDiff) / 2; result.push({ x: a.x, y: a.y + bias }, { x: b.x, y: b.y - bias }) }
    else { const bias = -xDiff / 2; result.push({ x: a.x + bias, y: a.y + bias }, { x: b.x - bias, y: b.y - bias }) }
  } else if (posRel === 'uur') {
    if (type === 'top') { const bias = -xDiff - yDiff; result.push({ x: b.x, y: b.y - bias }) }
    else if (type === 'bottom') { const bias = -xDiff - yDiff; result.push({ x: a.x, y: a.y + bias }) }
    else if (type === 'midInc') { const bias = (-xDiff - yDiff) / 2; result.push({ x: a.x, y: a.y + bias }, { x: b.x, y: b.y - bias }) }
    else { const bias = -xDiff / 2; result.push({ x: a.x + bias, y: a.y - bias }, { x: b.x - bias, y: b.y + bias }) }
  } else if (posRel === 'urr') {
    if (type === 'top') { const bias = xDiff + yDiff; result.push({ x: a.x - bias, y: a.y }) }
    else if (type === 'bottom') { const bias = xDiff + yDiff; result.push({ x: b.x + bias, y: b.y }) }
    else if (type === 'midInc') { const bias = (xDiff + yDiff) / 2; result.push({ x: a.x - bias, y: a.y }, { x: b.x + bias, y: b.y }) }
    else { const bias = yDiff / 2; result.push({ x: a.x + bias, y: a.y - bias }, { x: b.x - bias, y: b.y + bias }) }
  }
  if (reversed) result.reverse()
  return result
}

function justifyIllPosedSegments(segs: FormalSeg[]) {
  if (segs.length <= 1) return
  segs.forEach((seg, index) => {
    if (!seg.ill) return
    if (index > 0 && index < segs.length - 1) {
      const previous = segs[index - 1], next = segs[index + 1]
      if (!previous.direct && !next.direct && previous.ill < seg.ill && next.ill < seg.ill) {
        const previousRef = previous.itp.at(-1) ?? previous.a
        const nextRef = next.itp[0] ?? next.b
        const intersection = lineIntersection(previousRef, previous.b, nextRef, next.a)
        if (intersection) seg.itp = [intersection]
      }
      return
    }
    let intersection: Coord | undefined
    if (index === segs.length - 1) {
      const previous = segs[index - 1]
      if (!previous.direct && previous.ill <= seg.ill && previous.ill < 2) {
        const neighbourRef = previous.itp.at(-1) ?? previous.a
        const thisRef = seg.itp.length > 1 ? seg.itp[0] : undefined
        intersection = justifyEnd(neighbourRef, seg.a, thisRef, seg.b)
      }
    } else {
      const next = segs[index + 1]
      if (!next.direct && next.ill <= seg.ill && next.ill < 2) {
        const neighbourRef = next.itp[0] ?? next.b
        const thisRef = seg.itp.length > 1 ? seg.itp[1] : undefined
        intersection = justifyEnd(neighbourRef, seg.b, thisRef, seg.a)
      }
    }
    if (intersection) seg.itp = [intersection]
  })
}

function justifyEnd(neighbourRef: Coord, shared: Coord, thisRef: Coord | undefined, tip: Coord): Coord | undefined {
  const neighbourWay = { x: shared.x - neighbourRef.x, y: shared.y - neighbourRef.y }
  if (magnitude(neighbourWay) < EPS) return undefined
  if (!thisRef) {
    if (pointLineDistance(tip, neighbourRef, shared) < EPS) return undefined
    const perpendicularTip = { x: tip.x - neighbourWay.y, y: tip.y + neighbourWay.x }
    return lineIntersection(neighbourRef, shared, tip, perpendicularTip)
  }
  const thisWay = { x: shared.x - thisRef.x, y: shared.y - thisRef.y }
  if (!isZero(neighbourWay.x * thisWay.x + neighbourWay.y * thisWay.y)) return undefined
  return lineIntersection(neighbourRef, shared, tip, { x: tip.x + thisWay.x, y: tip.y + thisWay.y })
}

function lineIntersection(a: Coord, b: Coord, c: Coord, d: Coord): Coord | undefined {
  const r = { x: b.x - a.x, y: b.y - a.y }, s = { x: d.x - c.x, y: d.y - c.y }
  const denominator = cross2(r, s)
  if (Math.abs(denominator) < EPS) return undefined
  const t = cross2({ x: c.x - a.x, y: c.y - a.y }, s) / denominator
  return { x: a.x + r.x * t, y: a.y + r.y * t }
}

function pointLineDistance(point: Coord, a: Coord, b: Coord) {
  const way = { x: b.x - a.x, y: b.y - a.y }
  const length = magnitude(way)
  return length < EPS ? magnitude({ x: point.x - a.x, y: point.y - a.y }) : Math.abs(cross2({ x: point.x - a.x, y: point.y - a.y }, way)) / length
}

function coord(point: Coord): Coord { return { x: point.x, y: point.y } }
function isZero(value: number) { return Math.abs(value) < EPS }
function sameCoord(a: Coord, b: Coord) { return isZero(a.x - b.x) && isZero(a.y - b.y) }
function cross2(a: Coord, b: Coord) { return a.x * b.y - a.y * b.x }
function magnitude(value: Coord) { return Math.hypot(value.x, value.y) }
function unit(value: Coord): Coord { const length = magnitude(value) || 1; return { x: value.x / length, y: value.y / length } }
function signWay(value: Coord): Coord { return { x: isZero(value.x) ? 0 : value.x > 0 ? 1 : -1, y: isZero(value.y) ? 0 : value.y > 0 ? 1 : -1 } }
function unit8(value: Coord): Coord { return unit(signWay(value)) }
function fmt(value: number) { const rounded = Math.round(value * 1e6) / 1e6; return Object.is(rounded, -0) ? '0' : String(rounded) }
''', encoding='utf-8')

          replace_once(
              'src/data/basemapPaths.ts',
              "import { uid } from './model'\n",
              "import { uid } from './model'\nimport { buildAarcBasemapPathD } from './aarcBasemapGeometry'\n",
          )
          replace_once(
              'src/data/basemapPaths.ts',
              "      return pid && Number.isFinite(x) && Number.isFinite(y) ? [{ id: pid, x, y } satisfies BasemapPathPoint] : []",
              "      const aarcPointId = Number(p.aarcPointId)\n      const aarcDir = p.aarcDir === 1 || p.aarcDir === '1' ? 1 : p.aarcDir === 0 || p.aarcDir === '0' ? 0 : undefined\n      return pid && Number.isFinite(x) && Number.isFinite(y) ? [{ id: pid, x, y, ...(Number.isFinite(aarcPointId) ? { aarcPointId } : {}), ...(aarcDir !== undefined ? { aarcDir } : {}), ...(p.aarcFree === true ? { aarcFree: true } : {}) } satisfies BasemapPathPoint] : []",
          )
          replace_once(
              'src/data/basemapPaths.ts',
              "    const zIndex = Number.isFinite(Number(raw.zIndex)) ? Number(raw.zIndex) : 0\n    const rawSource = raw.source && typeof raw.source === 'object' ? raw.source as Record<string, unknown> : undefined",
              "    const zIndex = Number.isFinite(Number(raw.zIndex)) ? Number(raw.zIndex) : 0\n    const rawGeometry = raw.geometry && typeof raw.geometry === 'object' ? raw.geometry as Record<string, unknown> : undefined\n    const geometry = rawGeometry?.kind === 'aarc' ? { kind: 'aarc' as const, lineTurnAreaRadius: finiteNonNegative(rawGeometry.lineTurnAreaRadius, 30), lineWidthBase: finitePositive(rawGeometry.lineWidthBase, 14), lineCarpetWiden: finiteNonNegative(rawGeometry.lineCarpetWiden, 7), backgroundColor: typeof rawGeometry.backgroundColor === 'string' && rawGeometry.backgroundColor ? rawGeometry.backgroundColor : '#ffffff', ...(rawGeometry.removeCarpet === true ? { removeCarpet: true } : {}) } : undefined\n    const lineCap = raw.lineCap === 'butt' || raw.lineCap === 'round' || raw.lineCap === 'square' ? raw.lineCap : undefined\n    const rawSource = raw.source && typeof raw.source === 'object' ? raw.source as Record<string, unknown> : undefined",
          )
          replace_once(
              'src/data/basemapPaths.ts',
              "    const normalized = { id, ...(typeof raw.name === 'string' && raw.name ? { name: raw.name } : {}), category, points, color, width, opacity, closed, isFilled, zIndex, visible: raw.visible !== false, locked: raw.locked === true, ...(raw.smooth === true ? { smooth: true } : {}), ...(source ? { source } : {}) } as NativeBasemapPath",
              "    const normalized = { id, ...(typeof raw.name === 'string' && raw.name ? { name: raw.name } : {}), category, points, color, width, opacity, closed, isFilled, zIndex, visible: raw.visible !== false, locked: raw.locked === true, ...(lineCap ? { lineCap } : {}), ...(geometry ? { geometry } : {}), ...(raw.smooth === true ? { smooth: true } : {}), ...(source ? { source } : {}) } as NativeBasemapPath",
          )
          replace_once(
              'src/data/basemapPaths.ts',
              "export function getBasemapPathD(path: BasemapPath): string {\n  if (!path.points.length) return ''\n  if ((path as NativeBasemapPath).smooth === true && (!path.closed || path.points.length >= 3)) return getSmoothBasemapPathD(path)",
              "export function getBasemapPathD(path: BasemapPath): string {\n  if (!path.points.length) return ''\n  if (path.geometry?.kind === 'aarc') return buildAarcBasemapPathD(path)\n  if ((path as NativeBasemapPath).smooth === true && (!path.closed || path.points.length >= 3)) return getSmoothBasemapPathD(path)",
          )
          replace_once(
              'src/data/basemapPaths.ts',
              "function finitePositive(value: unknown, fallback: number) { const n = Number(value); return Number.isFinite(n) && n > 0 ? n : fallback }\nfunction clamp(value: number, min: number, max: number, fallback: number) { return Number.isFinite(value) ? Math.max(min, Math.min(max, value)) : fallback }",
              "function finitePositive(value: unknown, fallback: number) { const n = Number(value); return Number.isFinite(n) && n > 0 ? n : fallback }\nfunction finiteNonNegative(value: unknown, fallback: number) { const n = Number(value); return Number.isFinite(n) && n >= 0 ? n : fallback }\nfunction clamp(value: number, min: number, max: number, fallback: number) { return Number.isFinite(value) ? Math.max(min, Math.min(max, value)) : fallback }",
          )

          aarc = Path('src/import-export/aarc.ts')
          text = aarc.read_text(encoding='utf-8')
          old_point = "return [{ id: `aarc-basemap-point-${sourceLineId}-${pointId}-${pointIndex}`, x: position[0], y: position[1] }]"
          new_point = "return [{ id: `aarc-basemap-point-${sourceLineId}-${pointId}-${pointIndex}`, x: position[0], y: position[1], aarcPointId: pointId, aarcDir: point.dir === 1 ? 1 as const : 0 as const, ...(point.free === true ? { aarcFree: true } : {}) }]"
          if text.count(old_point) != 1:
              raise SystemExit(f'aarc.ts terrain point match count={text.count(old_point)}')
          text = text.replace(old_point, new_point)
          old_metrics = "    const terrainMetrics = resolveAarcTerrainSourceMetrics(width.raw, source.config.lineWidth)\n    const path: BasemapPath = {"
          new_metrics = "    const terrainMetrics = resolveAarcTerrainSourceMetrics(width.raw, source.config.lineWidth)\n    const sourceTurnRadius = finiteNumber(source.config.lineTurnAreaRadius)\n    const sourceLineWidthBase = finiteNumber(source.config.lineWidth)\n    const sourceCarpetWiden = finiteNumber(source.config.lineCarpetWiden)\n    const terrainGeometry = { kind: 'aarc' as const, lineTurnAreaRadius: sourceTurnRadius !== null && sourceTurnRadius >= 0 ? sourceTurnRadius : 30, lineWidthBase: sourceLineWidthBase !== null && sourceLineWidthBase > 0 ? sourceLineWidthBase : 14, lineCarpetWiden: sourceCarpetWiden !== null && sourceCarpetWiden >= 0 ? sourceCarpetWiden : 7, backgroundColor: typeof source.config.bgColor === 'string' && source.config.bgColor ? source.config.bgColor : '#ffffff', ...(rawLine.removeCarpet === true ? { removeCarpet: true } : {}) }\n    const path: BasemapPath = {"
          if text.count(old_metrics) != 1:
              raise SystemExit(f'aarc.ts terrain metrics match count={text.count(old_metrics)}')
          text = text.replace(old_metrics, new_metrics)
          old_tail = "zIndex, visible: true, locked: false, source: { format: 'aarc', sourceLineId,"
          new_tail = "zIndex, visible: true, locked: false, ...(rawLine.cap === 'butt' || rawLine.cap === 'round' || rawLine.cap === 'square' ? { lineCap: rawLine.cap } : {}), geometry: terrainGeometry, source: { format: 'aarc', sourceLineId,"
          if text.count(old_tail) != 1:
              raise SystemExit(f'aarc.ts path tail match count={text.count(old_tail)}')
          text = text.replace(old_tail, new_tail)
          aarc.write_text(text, encoding='utf-8')

          renderer = Path('src/renderer/BasemapPaths.tsx')
          text = renderer.read_text(encoding='utf-8')
          old_render = """    const suppressFilledStroke = path.isFilled && path.source?.format === 'aarc'
    return <g data-basemap-path-id={path.id} data-z-index={path.zIndex} className={`basemap-path ${selected ? 'selected' : ''}`}>
      <path d={d} fill={path.isFilled ? path.color : 'none'} fillOpacity={path.isFilled ? path.opacity : 0} stroke={suppressFilledStroke ? 'none' : path.color} strokeWidth={suppressFilledStroke ? 0 : path.width} strokeOpacity={suppressFilledStroke ? 0 : path.opacity} strokeLinecap=\"round\" strokeLinejoin=\"round\" pointerEvents={presentation || path.locked ? 'none' : path.isFilled ? 'all' : 'stroke'} onPointerDown={event => onPathPointerDown?.(event, path)} />"""
          new_render = """    const suppressFilledStroke = path.isFilled && path.source?.format === 'aarc'
    const aarcGeometry = path.geometry?.kind === 'aarc' ? path.geometry : undefined
    const carpetWidth = aarcGeometry ? path.isFilled ? aarcGeometry.lineWidthBase * .5 : path.width + aarcGeometry.lineCarpetWiden : 0
    const showCarpet = Boolean(aarcGeometry && !aarcGeometry.removeCarpet && carpetWidth > 0)
    return <g data-basemap-path-id={path.id} data-z-index={path.zIndex} className={`basemap-path ${selected ? 'selected' : ''}`}>
      {showCarpet && <path data-aarc-terrain-carpet=\"true\" d={d} fill=\"none\" stroke={aarcGeometry!.backgroundColor} strokeWidth={carpetWidth} strokeLinecap=\"round\" strokeLinejoin=\"round\" pointerEvents=\"none\" />}
      <path d={d} fill={path.isFilled ? path.color : 'none'} fillOpacity={path.isFilled ? path.opacity : 0} stroke={suppressFilledStroke ? 'none' : path.color} strokeWidth={suppressFilledStroke ? 0 : path.width} strokeOpacity={suppressFilledStroke ? 0 : path.opacity} strokeLinecap={path.lineCap ?? 'round'} strokeLinejoin=\"round\" pointerEvents={presentation || path.locked ? 'none' : path.isFilled ? 'all' : 'stroke'} onPointerDown={event => onPathPointerDown?.(event, path)} />"""
          if text.count(old_render) != 1:
              raise SystemExit(f'BasemapPaths render match count={text.count(old_render)}')
          renderer.write_text(text.replace(old_render, new_render), encoding='utf-8')

          terrain_test = Path('src/import-export/aarcTerrain.test.ts')
          text = terrain_test.read_text(encoding='utf-8')
          import_anchor = "import { isValidAarcTerrainColor, parseAarcTerrainWidth, resolveAarcTerrainAppearance, resolveAarcTerrainPreset, resolveAarcTerrainSourceMetrics } from './aarcTerrain'\n"
          if text.count(import_anchor) != 1:
              raise SystemExit('aarcTerrain.test import anchor mismatch')
          text = text.replace(import_anchor, import_anchor + "import { getBasemapPathD } from '../data/basemapPaths'\nimport { formalizeAarcBasemapPoints } from '../data/aarcBasemapGeometry'\n")
          insert_before = "  it('preserves 平岚 terrain colors, widths, stacking and every source point', () => {"
          compatibility_tests = r'''  it('preserves AARC dir/free semantics and reconstructs implicit terrain geometry', () => {
    const raw = {
      cvsSize: [100, 100],
      config: { lineWidth: 10, lineTurnAreaRadius: 30, lineCarpetWiden: 6, bgColor: '#f5f5f5' },
      points: [
        { id: 1, pos: [0, 0], sta: 0, dir: 0 },
        { id: 2, pos: [20, 10], sta: 0, dir: 0 },
      ],
      lines: [{ id: 20, name: 'T', type: 1, pts: [1, 2], width: 1, color: '#123456', cap: 'butt' }],
    }
    const { project } = convertAarcToActualRouteProject(raw)
    const path = project.basemapPaths![0]
    expect(path.points).toMatchObject([
      { aarcPointId: 1, aarcDir: 0 },
      { aarcPointId: 2, aarcDir: 0 },
    ])
    expect(path.geometry).toEqual({ kind: 'aarc', lineTurnAreaRadius: 30, lineWidthBase: 10, lineCarpetWiden: 6, backgroundColor: '#f5f5f5' })
    expect(path.lineCap).toBe('butt')
    expect(formalizeAarcBasemapPoints(path.points).map(point => [point.x, point.y])).toEqual([[0, 0], [5, 0], [15, 10], [20, 10]])
    expect(getBasemapPathD(path)).toContain(' A ')
    expect(getBasemapPathD(path)).not.toBe('M 0 0 L 20 10')
  })

  it('keeps free-point terrain legs direct while retaining arbitrary-angle rounding', () => {
    const raw = {
      cvsSize: [120, 120],
      points: [
        { id: 1, pos: [0, 0], sta: 0, dir: 0 },
        { id: 2, pos: [23, 17], sta: 0, dir: 1, free: true },
        { id: 3, pos: [80, 17], sta: 0, dir: 0 },
      ],
      lines: [{ id: 20, name: 'free terrain', type: 1, pts: [1, 2, 3], width: 1, color: '#123456' }],
    }
    const path = convertAarcToActualRouteProject(raw).project.basemapPaths![0]
    expect(path.points[1]).toMatchObject({ aarcPointId: 2, aarcDir: 1, aarcFree: true })
    expect(formalizeAarcBasemapPoints(path.points).map(point => [point.x, point.y])).toEqual([[0, 0], [23, 17], [80, 17]])
    expect(getBasemapPathD(path)).toContain(' A ')
  })

'''
          if text.count(insert_before) != 1:
              raise SystemExit('aarcTerrain.test insertion anchor mismatch')
          terrain_test.write_text(text.replace(insert_before, compatibility_tests + insert_before), encoding='utf-8')

          renderer_test = Path('src/renderer/BasemapPaths.test.tsx')
          text = renderer_test.read_text(encoding='utf-8')
          end_anchor = "\n})\n"
          if not text.endswith(end_anchor):
              raise SystemExit('BasemapPaths.test ending mismatch')
          extra = r'''

  it('renders AARC terrain carpet and explicit endpoint cap from preserved source semantics', () => {
    const project = createEmptyProject()
    project.basemapPaths = [{ id: 'aarc-stroke', category: 'water', points: [{ id: 'a', x: 0, y: 0, aarcPointId: 1, aarcDir: 0 }, { id: 'b', x: 40, y: 0, aarcPointId: 2, aarcDir: 0 }], color: '#123456', width: 20, opacity: 1, closed: false, isFilled: false, zIndex: 0, visible: true, locked: false, lineCap: 'butt', geometry: { kind: 'aarc', lineTurnAreaRadius: 30, lineWidthBase: 10, lineCarpetWiden: 7, backgroundColor: '#eeeeee' }, source: { format: 'aarc', sourceLineId: 20 } }]
    const { container } = render(createElement('svg', null, createElement(BasemapPathsLayer, { project })))
    const group = container.querySelector('[data-basemap-path-id="aarc-stroke"]')!
    const carpet = group.querySelector('[data-aarc-terrain-carpet="true"]')!
    const body = group.querySelector('path:not([data-aarc-terrain-carpet])')!
    expect(carpet.getAttribute('stroke')).toBe('#eeeeee')
    expect(carpet.getAttribute('stroke-width')).toBe('27')
    expect(carpet.getAttribute('stroke-linecap')).toBe('round')
    expect(body.getAttribute('stroke-linecap')).toBe('butt')
  })
'''
          renderer_test.write_text(text[:-len(end_anchor)] + extra + end_anchor, encoding='utf-8')
          PY
      - name: Install
        run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Test
        run: npm test -- --run
      - name: Build web
        run: npm run build:web
      - name: Commit validated patch and remove temporary workflow
        shell: bash
        run: |
          rm .github/workflows/temp-aarc-terrain-geometry.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix AARC terrain geometry fidelity"
          git push origin HEAD:fix/aarc-terrain-geometry-fidelity
