# 移动端构建

Web：`dist/index.html`

Android APK：运行 `npm run android:apk`，产物为 `release/actual-route-editor-debug.apk`。

ADB 安装：`npm run android:install`

Android Studio：`npm run android:open`

PWA：运行 `npm run build:pwa`，产物目录为 `dist-pwa/`。
